# CODEX_SESSION.md — 작업 세션 절차와 기록

새 Codex 세션은 이 문서를 읽고 시작한다.
규칙은 `AGENTS.md`, 협업 배경은 `CODEX_CLAUDE_HANDOFF.md`에 있다.

---

## 현재 상태

> 세션이 끝날 때마다 이 블록을 갱신한다.

- GitHub `main`: **`A-0.1.49` / versionCode `57`** (커밋 `c3e7dd6`, push 2026-07-27)
- 실기기 설치본: `A-0.1.49` / versionCode `57` (`monster` 변형)
- 저장소 공개 상태: **public**
- 로컬 저장소 경로: `C:\Users\baboe\Documents\Codex\monster-touch`
- 기본 설치 대상은 `plain`이지만 실기기에는 `monster`가 설치돼 있음

> ✅ **소스 복구 완료 (2026-07-27)**
> `A-0.1.39`~`A-0.1.49` 소스가 GitHub에 없어 다른 컴퓨터의 ZIP으로 복구했다.
> 이 트리로 빌드한 APK의 `classes.dex` / `classes2.dex`가 실기기에서 뽑은 APK와
> **바이트 단위로 동일**함을 확인했다. 유실된 코드 없음. 커밋·푸시 완료.

### 진행 중인 설계 — 액션/슬롯 분리

"위치 표시"와 "화면 잠금"이 슬롯 번호에 하드코딩돼 있다
(`POSITION_TOGGLE_SLOT = 0`, `LOCK_SLOT = 3`). 실제 하드웨어에서는 그 두 동작이
버튼 1/4의 롱클릭이 아닌 경우가 많다. 액션을 1급 개념으로 올리는 리팩터가 필요하다.

- 액션 6개: 탭 1~4, 위치 표시 토글, 화면 잠금 토글
- 각 액션이 독립 바인딩 `{입력 신호, 롱클릭 여부}`를 가짐
- 저장은 기존 스키마 확장(`trigger_*_4`, `trigger_*_5`) — 시그니처 포맷 불변
- 마이그레이션 v4:
  1. `long_trigger_*`가 있으면 그대로 이전 (LP-910: VOLUME_UP/DOWN)
  2. 없고 슬롯 0/3이 키 타입이면 암묵 롱클릭을 명시 바인딩으로 실체화 (HID 키보드)
  3. 둘 다 아니면 미할당

기기 타입별 근거:
- HID 키보드는 길게 눌러도 다른 키로 변환되지 않아 같은 키의 롱클릭이 실제로 들어간다
- LP-910은 롱클릭이 별개 채널의 볼륨키로 나온다 (롱클릭 개념 없음)
- 가상 터치(`d348a3a07`)는 롱 트리거가 하나도 없어 **현재 표시/잠금을 켤 방법이 없다**

### 진행 중인 설계 — UI/UX 정리

앱은 기능적으로 잘 동작한다. 문제는 배치와 표현이다.
기능을 늘리지 말고 **있는 것을 더 잘 배치하는** 방향으로 간다.

가장 큰 불편: 매핑 행마다 `[키 입력] [Long] [이름 변경]` 세 버튼이 같이 있는데
성격이 전혀 다르다. 입력 바인딩 / 별개 액션의 바인딩 / 라벨 편집이 같은 무게로 놓여 있다.

**제안: 행 전체를 터치 타겟으로.**

```
현재                                제안
버튼 1 · Button 1                   버튼 1
키: Mouse Left B|a=11,0|e=4,0|...   마우스 왼쪽 · 709, 1095      ›
위치: 709, 1095                     ← 행 전체가 타겟, 탭하면 상세
[키 입력][Long][이름 변경]
```

상세는 `AlertDialog` (이미 8곳에서 쓰고 있어 일관됨): 입력 등록 / 이름 변경 / 이 버튼 초기화.

- 작은 버튼 12개 → 0개. 타겟이 폭 전체 × 높이 ~72dp가 되어 장갑에 유리
- 편집이 한 단계 안쪽이라 빗속 오조작이 줄어듦
- 액션이 6개로 늘어도 화면이 안 무너짐

**좌표 설정은 지금 흐름을 유지한다.** 상단 `위치 설정` → `showSetupOverlay()` →
`moveTaskToBack(true)` → 떠 있는 패널을 드래그로 옮기며 원하는 앱 화면에서 버튼을 누름.
배민 화면을 보면서 찍어야 하므로 설정 앱 안의 다이얼로그로 옮기면 안 된다.
사용자도 이 흐름은 불편하지 않다고 확인했다.

같이 정리할 것:

- **시그니처 문자열 노출 제거.** `키: Mouse Left B|a=11,0|e=4,0|d=2|t=M|m=6`은 디버그
  정보다. `마우스 왼쪽`이면 충분하다. 진단 화면에만 남긴다.
- **표시 형식 불일치.** 샘플이 1개면 시그니처 전체, 2개 이상이면 `samples:N`으로
  다르게 나온다. 하나로 통일한다.
- **`키:` 라벨이 틀렸다.** LP-910은 전부 마우스 제스처인데 "키"로 표시된다.
- **`버튼 1 · Button 1`** — 기본 이름이 영문이라 같은 말이 반복된다. 이름을 안 바꿨으면
  `버튼 1`만 표시한다. 좌표 설정 오버레이 패널에도 같은 중복이 있다.
- **상태 줄이 빽빽하다.** `접근성 ON · 위치 표시 ON · 터치 잠금 OFF · 2초 길게 누르기: 1 위치표시, 4 잠금`
  — 상태 세 개와 사용법 안내가 한 줄에 섞여 있다.
- **개발자 정보 노출.** `처리 안내: 모션 캡처 (setMotionEventSources) + 키 이벤트`는
  API 이름이 그대로 보인다. 접어둔다.
- **`위치 표시 켜짐` 버튼 라벨이 모호하다.** 현재 상태인지 누르면 될 일인지 불명확.
- **라벨 언어 혼재.** `"Long"`, `"Press and hold the remote button."`만 영문이다.

작업 순서: ① 행 → 탭 타겟 + 시그니처 숨기기 → ② 상태 줄 정리 → ③ 액션/슬롯 분리

### 테스트 기기

- FYD IM-H842 / Android 15 (SDK 35) / arm64-v8a / 1200 × 1920, density 280
- 무선 디버깅 (`adb connect 192.168.1.38:35981`), 접근성 서비스 활성화됨
- 선택된 컨트롤러: **UGREEN LP-910** (`input_device_mode = MOTION`)

### 등록된 컨트롤러 3종

프로필 키는 `"d" + hex(("descriptor:" + 디스크립터).hashCode())`.

| 프로필 키 | 기기 | 방식 |
| --- | --- | --- |
| `dde3b4fa8` | HID 키보드 | 키코드 38 / 35 / 36 / 37 |
| `d5fae8545` | **UGREEN LP-910** (현재 선택) | 모션 버스트 + 볼륨키 |
| `d348a3a07` | ATG-SJL (가상 터치) | 모션 버스트 + 키 25 |

### LP-910 하드웨어 사실 (커널 캡처 + 제조사 매뉴얼)

물리 버튼 6개:

```
上翻页  단click: 페이지 위    길게: 음량 +
下翻页  단click: 페이지 아래  길게: 음량 −
左翻页  단click: 페이지 왼쪽
右翻页  단click: 페이지 오른쪽
鼠标键  단click: 서드파티 앱 사진/녹화
电源键  단click: 사진/녹화    길게: 전원 (사용 금지 — 리모컨이 꺼짐)
```

입력 기기가 **두 개**로 잡힌다. 하나의 물리 기기인데 채널이 둘이다.

```
/dev/input/event6  UGREEN-LP910 Mouse             CURSOR    → MotionEvent
/dev/input/event5  UGREEN-LP910 Consumer Control  KEYBOARD  → KeyEvent (볼륨키)
둘 다 vendor=0x0E05 product=0x0A00
```

`acceptsInputDevice`의 vendor/product 폴백 덕분에 하나만 선택해도 양쪽이 통과한다.

**모든 버튼 입력 앞에 홈잉 시퀀스가 붙는다:**

```
5회:  REL_X = -2047,  REL_Y = +2047     ← 커서를 구석으로 슬램 (절대 위치 리셋)
1회:  REL_X = +170,   REL_Y = -241      ← 기준점으로 이동
```

이어서 버튼별 패턴:

```
A: REL_X +54 ×3 → BTN DOWN → REL_X -54 ×6 → BTN UP
B: BTN DOWN → (+2,+3) → REL_Y +50 ×3 → BTN UP
C: REL_X -54 ×3 → BTN DOWN → REL_X +54 ×6 → BTN UP
D: BTN DOWN → (+2,-3) → REL_Y -50 ×3 → BTN UP
```

**같은 버튼은 매번 바이트 단위로 동일한 이벤트를 낸다.** 하드웨어는 완벽히 결정적이다.
앱에서 보이는 흔들림은 Android 포인터 파이프라인(가속 · 클램핑 · 이벤트 병합)에서 생긴다.

### 커서가 휙휙 지나가는 것은 없앨 수 없다

이 기기는 `翻页`(페이지 넘김)를 **커서를 옮겨서 클릭하는 방식**으로 구현한다.
커서 이동이 부작용이 아니라 신호 그 자체다. 없애면 버튼 4개가 전부 같은 자리를
클릭하게 되어 구분이 불가능해진다.

앱 쪽 수단도 없다. `setMotionEventSources()`는 이벤트 사본을 받을 뿐 시스템이 커서를
옮기는 것을 막지 못한다. `setPointerIcon(투명)`은 포인터가 그 뷰 위에 있을 때만
적용되는데, 홈잉은 화면 전체를 가로지르므로 전체 화면 뷰가 필요하다. 그런데 전체 화면
트랩은 `AGENTS.md` §3 / §5가 마우스류에 대해 금지한다 (터치스크린이 막힘).

미검증 가능성: `FLAG_NOT_TOUCHABLE` 전체 화면 오버레이면 터치를 통과시키면서 포인터
아이콘만 바꿀 수 있는가? Android가 NOT_TOUCHABLE 창을 포인터 아이콘 결정에서 제외할
가능성이 높다고 보지만 확인된 바 없다. 성공해도 터치 차단 위험이 있어 권하지 않는다.

### `鼠标键` 캡처 결과 (2026-07-27)

짧게 3회 / 길게 3회 눌러 확인했다.

```
짧게:  홈잉 → 오프셋 (+170, -121) → BTN_MOUSE DOWN → UP (33ms, 사이에 이동 없음)
길게:  홈잉 → 오프셋. 끝. BTN_MOUSE 없음
```

**짧게 누름은 쓸 수 있다.** D-pad와 확실히 구분된다.

| | 앵커 오프셋 | 클릭 중 이동 `m` | 지속 `t` |
| --- | --- | --- | --- |
| D-pad 버튼들 | −241 | 4~6 | M |
| `鼠标键` | **−121** | **0** | **S** |

`m` 차이만으로 거리 8점(임계값 5 초과), `t`까지 더하면 10점이다. 슬롯 3(下翻页)도
STILL로 기록되지만 `m=4` vs `m=0`으로 갈린다.

**길게 누름은 쓸 수 없다.** 홈잉만 나오는데 이는 `buttonState = 0`인 hover move라
`MotionBurst.isStartAction()`이 false를 반환한다. 버스트가 시작조차 안 된다.

활용 방안 (다음에 논의): 위치 표시나 잠금 중 하나를 `鼠标键` 즉시 누름으로 옮기면
2초 롱프레스 대기가 사라진다. 어느 쪽을 옮길지는 미정 — 잠금은 오조작이 곤란하므로
오히려 2초가 안전장치일 수 있다.

### 알려진 미해결 문제

- **`README.md` 버전 갱신 완료** (`A-0.1.49` / `57`)
- `CODEX_HANDOFF_A.md` 내용이 오래됨 → 확인 필요
- ADB probe 리포트가 기기에 한 번도 저장된 적 없음 (`getExternalFilesDir` 경로 미생성)
- 가상 터치 기기(`d348a3a07`)는 롱 트리거가 없어 표시/잠금을 켤 방법이 현재 없음

---

## 세션 시작 체크리스트

```powershell
"C:\Program Files\Git\cmd\git.exe" pull
"C:\Program Files\Git\cmd\git.exe" status
```

1. `git status`가 깨끗한지 확인한다. 남은 변경이 있으면 **먼저 사람에게 묻는다.**
   다른 컴퓨터에서 하던 작업일 수 있다.
2. `app/build.gradle`에서 현재 `versionName` / `versionCode`를 읽고
   위 "현재 상태" 블록과 일치하는지 확인한다. 다르면 문서를 먼저 맞춘다.
3. `adb devices`로 실기기 연결을 확인한다. 연결이 없으면
   빌드까지만 하고 "설치·검증 못 함"을 명시한다.
4. 이번 세션에서 할 일 한 가지를 정한다. 여러 개를 한 번에 섞지 않는다.

---

## 작업 루프

한 번에 한 가지 변경만. 아래 순서를 지킨다.

1. **변경 전 계획을 한 줄로 적는다** — 무엇을, 왜, 어느 파일에서.
2. `app/` 이하만 수정한다. (`checkpoints/`, `outputs/`는 건드리지 않는다.)
3. `versionName` + `versionCode`를 올린다.
4. 빌드한다.
   ```powershell
   .\gradlew.bat assemblePlainDebug
   ```
5. 설치한다.
   ```powershell
   adb install -r app\build\outputs\apk\plain\debug\app-plain-debug.apk
   ```
6. 앱을 열어 **화면의 versionName이 방금 올린 값인지 확인한다.**
7. 실기기에서 실제 컨트롤러로 동작을 확인한다.
8. 커밋 & push.
9. 아래 형식으로 보고한다.

---

## 보고 형식

```text
버전: A-0.1.N (versionCode M)
변경: (한 줄)
수정 파일: (경로 목록)

빌드: 성공 / 실패
설치: 성공 / 실패 / 안 함
기기: (모델, Android 버전)
컨트롤러: (모델명)

실기기에서 확인한 것:
- ...

확인하지 못한 것:
- ...

남은 문제 / 다음에 할 일:
- ...
```

"확인하지 못한 것"을 비워두지 않는다. 없으면 "없음"이라고 적는다.

---

## 사람에게 먼저 물어야 하는 경우

아래는 Codex가 임의로 진행하지 않고 확인을 받는다.

- 시그니처 문자열 포맷 변경 (기존 사용자 매핑이 전부 무효화된다)
- 양자화 bins(12 × 16), 거리 임계값(≤5), 샘플 수(5), 쿨다운(220ms) 변경
- 터치 잠금 동작 변경
- `applicationId` / `namespace` / flavor 구성 변경
- 새 권한 추가 (특히 `SYSTEM_ALERT_WINDOW`)
- `minSdk` 상향
- `outputs/`나 `checkpoints/`의 기존 파일 삭제·정리
- 저장소 공개 설정 변경

---

## Claude에게 넘길 때

다음 상황이면 코드를 더 고치기 전에 Claude 리뷰를 요청한다.

- 같은 증상을 두 번 고쳤는데 또 재발할 때
- 입력 매칭 구조 자체를 바꿔야 할 것 같을 때
- 접근성 정책이나 Android 버전별 동작 차이가 얽힐 때
- 특정 기기·OEM에서만 재현되는 문제일 때

요청에 포함할 것: 현재 버전, 바꾼 함수, 실기기 증상, 컨트롤러 모델,
Android 버전, 진단 화면(Controller Analyzer / ADB Probe) 기록.

---

## 세션 종료 체크리스트

1. 위 "현재 상태" 블록을 갱신한다.
2. `README.md` 버전 표기가 맞는지 확인한다.
3. 아래 순서로 마무리한다.
   ```powershell
   "C:\Program Files\Git\cmd\git.exe" status
   "C:\Program Files\Git\cmd\git.exe" add .
   "C:\Program Files\Git\cmd\git.exe" commit -m "A-0.1.N: 요약"
   "C:\Program Files\Git\cmd\git.exe" push
   ```
4. APK를 Google Drive `CODEX/monster-touch/01_APK/`에 올린다.
5. 미완료 작업과 다음 세션에서 이어서 할 일을 위 블록에 적는다.

---

## 세션 기록

> 최신 항목을 위에 추가한다. 오래된 항목은 지우지 않는다.

### 2026-07-27

**소스 복구**
- 저장소를 public으로 전환 (시크릿 스캔: 키스토어·자격증명 없음).
- 저장소가 `A-0.1.38`인데 실기기에 `A-0.1.49`가 설치돼 있는 것을 발견.
  이 컴퓨터 전체를 검색했으나 `.39`~`.49` 소스 없음. Google Drive는 로그인이 필요해
  확인 불가. 폰에서 APK를 추출해 jadx로 디컴파일까지 진행했으나,
  다른 컴퓨터의 ZIP으로 진짜 소스를 받아 해결.
- 복구본으로 빌드한 APK의 dex가 폰에서 뽑은 APK와 **바이트 단위 동일**함을 확인.
- 커밋 `c3e7dd6`으로 push 완료. `AGENTS.md`도 저장소에 포함됨.
- 이 사고를 근거로 `CODEX_CLAUDE_HANDOFF.md`에 "작업 완료 시 GitHub 반영" 규칙 추가.

**입력 분석**
- LP-910을 무선 디버깅으로 연결해 `getevent`로 커널 레벨 캡처.
- 홈잉 시퀀스, 버튼별 결정적 패턴, 이중 채널 구조를 확인 (위 참고).
- `鼠标键` 캡처 — 짧게 누름은 사용 가능, 길게 누름은 신호 없음.

**볼륨키 억제 건 — 조사 후 취소**
- `remoteVolumeSuppressUntilMs`(1800ms)가 홈잉 hover move에도 걸려 롱프레스 토글이
  지연될 수 있다고 보았으나, **실사용에서 체감되지 않는다고 확인되어 수정하지 않기로 함.**
- `VOLUME_KEY_DEBOUNCE_MS = 1000`은 버그가 아니라 **의도된 장치**다. 버튼을 길게 누르면
  볼륨키가 연속으로 쏟아지는데, 토글에 그대로 연결하면 켜졌다 꺼졌다를 반복한다.
  **건드리지 말 것.**

**나중에 볼 것 (지금은 문제 없음)**
- `m`(이동 이벤트 수)은 매우 안정적이다. 슬롯별 6/4/6/4로 한 번도 안 흔들렸다.
  버튼 구분에 좋은 특징이므로 거리 계산에서 빼지 말 것.
- `t`(누른 시간 버킷)는 노이즈다. 다른 필드가 전부 같고 `t`만 다른 샘플이 두 건 있다.
- 앵커(`a`)는 흔들린다. 슬롯 2에 `a=2,0`과 `a=8,0`이 공존한다 (6 bin = 600px 차이).
- 방향 판정 절벽: `motionBurstDirection`은 `max(|dx|,|dy|) <= 1`이면 STILL을 반환하는데
  `findClosestMappedMouseGesture`는 방향이 다르면 후보에서 아예 제외한다(하드 게이트).
  슬롯 1(DOWN)과 슬롯 3(STILL)은 앵커가 같고 종점 y만 3 bin 차이라 오인 여지가 있다.

**미반영**
- 이 문서와 `CODEX_CLAUDE_HANDOFF.md`를 저장소에 커밋 (아직 로컬에만 있음).
- UI/UX 정리 착수 (설계는 위에 정리됨).
