<?xml version="1.0" encoding="utf-8"?>
<!--
    1x1 크기다. minWidth/minHeight는 70*칸수-30 공식으로 40dp가 된다.

    updatePeriodMillis는 안드로이드가 30분 아래로 내려주지 않으므로 전류를 보여주는 데는
    쓸 수 없다. 실제 갱신은 이미 돌고 있는 접근성 서비스가 밀어넣고, 이 주기는 서비스가
    꺼져 있을 때 위젯이 영영 멈춰 있지 않도록 두는 최소한의 보험이다.

    답답하면 늘릴 수 있도록 크기 조절은 열어둔다.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:minResizeWidth="40dp"
    android:minResizeHeight="40dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_battery"
    android:previewLayout="@layout/widget_battery"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
