[env:c3]
platform = espressif32
board = esp32-c3-devkitm-1
framework = arduino

upload_port = COM12
monitor_port = COM12
monitor_speed = 115200

; 이 보드는 네이티브 USB로 시리얼이 올라온다. CDC를 켜야 로그가 보인다.
build_flags =
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1
